<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title, id: :comment_title %>
  </div>

    <div class="field">
      <%= form.hidden_field :user_id,:value=>current_user.id, id: :campaign_user_id %>

    </div>
    <% if params[:campaign_id].present? %>
      <%= form.hidden_field :campaign_id,:value=>params[:campaign_id], id: :campaign_comment_id %>
    <% elsif params[:todo_id].present? %>
       <%= form.hidden_field :todo_id,:value=>params[:todo_id], id: :todo_user_id %>
    <%end%>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
